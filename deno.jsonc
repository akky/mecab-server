{
    "tasks": {
        "server-on-docker": "docker-compose up",
        "server": "deno run  --allow-run --allow-net=0.0.0.0 --allow-read=. --allow-env main.ts",
        "test": "deno test --allow-run --allow-net=localhost --allow-read=.",
    }
}